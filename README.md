Clickbait Verifier - minimal runnable skeleton generated from system spec.

Run:
- Install requirements: pip install -r requirements.txt
- Configure sources in config.yaml
- Run: python -m clickbait_verifier.main
