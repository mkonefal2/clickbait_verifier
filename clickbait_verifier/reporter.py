import duckdb
import pandas as pd
from datetime import datetime


"""Reporter removed — scraping-only project."""

def generate_daily_report(*args, **kwargs):
    raise RuntimeError('Reporter disabled in scraping-only mode')
