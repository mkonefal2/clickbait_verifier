"""IMPORTER REMOVED: This repository was simplified to scraping-only.
If you need to re-enable import of analyzed results, restore the original script from history.
"""

def import_results(path):
    raise RuntimeError('import_agent_results.py has been removed in scraping-only mode')


if __name__ == '__main__':
    print('import_agent_results.py is disabled in scraping-only mode')
